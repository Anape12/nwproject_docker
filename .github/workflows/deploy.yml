name: Deploy to Raspberry Pi

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ソース取得
      - uses: actions/checkout@v4

      # Javaセットアップ
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      # Tailscale接続
      - name: Connect Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_CLIENT_SECRET }}
          tags: tag:ci

      # Tailscale状態確認（参考情報）
      - name: Check Tailscale
        run: tailscale status

      # Raspberry Pi疎通確認
      - name: Check Raspberry Pi
        id: check_pi
        run: |
          if ping -c 3 ${{ secrets.HOST }} >/dev/null 2>&1; then
            echo "online=true" >> $GITHUB_OUTPUT
            echo "Raspberry Pi is ONLINE"
          else
            echo "online=false" >> $GITHUB_OUTPUT
            echo "Raspberry Pi is OFFLINE"
          fi

      # SSHポート確認
      - name: Check SSH Port
        if: steps.check_pi.outputs.online == 'true'
        run: |
          nc -zv ${{ secrets.HOST }} 22

      # テスト・ビルド・デプロイ
      - name: Test & Deploy
        if: steps.check_pi.outputs.online == 'true'
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 20m
          script: |
            set -e

            cd ~/workspace/nwproject_docker

            echo "===== Git Pull ====="
            git pull

            chmod +x mvnw

            echo "===== Unit Test ====="
            ./mvnw test

            echo "===== Build ====="
            ./mvnw clean install -DskipTests

            echo "===== Docker Deploy ====="
            cd .devcontainer
            docker compose up -d --build

            echo "===== Container Status ====="
            docker compose ps

            echo "===== Health Check ====="

            READY=false

            for i in {1..30}; do
              if curl -fsL http://localhost:8080/Login >/dev/null 2>&1; then
                READY=true
                break
              fi

              echo "Waiting... ($i/30)"
              sleep 2
            done

            if [ "$READY" = false ]; then
              echo "===== Tomcat Logs ====="
              docker logs my-tomcat --tail 200

              echo "===== Flyway Logs ====="
              docker logs devcontainer-flyway-1 --tail 100 || true

              exit 1
            fi

            echo "===== Deploy Complete ====="

      # Pi停止時のメッセージ
      - name: Skip Deploy
        if: steps.check_pi.outputs.online != 'true'
        run: |
          echo "======================================="
          echo " Raspberry Pi is offline."
          echo " Deploy has been skipped."
          echo "======================================="